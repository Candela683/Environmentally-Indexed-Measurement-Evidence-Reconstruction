# NetCDF Subset Placeholder

This synthetic demo does not require NetCDF files. It stores the required specific year-month environmental values directly in the SQLite table `environment_monthly`.

If a future test needs NetCDF, place only the small year-month slices used by the synthetic PDF records in this folder. Do not copy large CMEMS product archives here.
